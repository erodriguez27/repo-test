<core:FragmentDefinition xmlns:core="sap.ui.core" xmlns="sap.m">
  <Popover id="popoverCapacity6" placement="HorizontalPreferredRight" showHeader="false" class="sapUiContentPadding">
    <Table id="inventoryDTable6" inset="false" items="{path:'mdBroilerEvictionMonitor>/dataPopover'}" selectionChange="onSelectProgrammedRecord">
      <columns>
        <Column hAlign="Center">
          <Text text="Producto"/>
        </Column>
        <!-- <Column minScreenWidth="Tablet" demandPopin="true" hAlign="Center">
          <Text text="Cantidad"/>
        </Column> -->
      </columns>

      <items>
        <ColumnListItem>
          <cells>
  					<ObjectNumber number="{mdBroilerEvictionMonitor>name}" />
            <!-- <ObjectNumber number="{path:'mdBroilerEvictionMonitor>scheduled_quantity'}" unit="Pollitos"/> -->
          </cells>
        </ColumnListItem>
      </items>
    </Table>
  </Popover>
</core:FragmentDefinition>

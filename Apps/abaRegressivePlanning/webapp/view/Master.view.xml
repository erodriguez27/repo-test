<mvc:View xmlns:core="sap.ui.core" xmlns:mvc="sap.ui.core.mvc" xmlns="sap.m" xmlns:html="http://www.w3.org/1999/xhtml"
          xmlns:u="sap.ui.unified"
          controllerName="abaRegressivePlanning.controller.Master">
    <App>
        <pages>
            <Page title="Escenarios">
                <headerContent>
                    <!-- <Button text="{data>/compareButton/text}" press="activateComparation" -->
                            <!-- type="{data>/compareButton/type}"/> -->
                    <!-- <Button text="{data>/calculateButton/text}" press="calculate" type="{data>/calculateButton/type}" -->
                            <!-- visible="{data>/calculateButton/visible}"/> -->
                </headerContent>
                <List id="scenarioList" items="{data>/scenarios}" mode="{data>/listType}"
                      selectionChange="onSelectionChange">
                    <items>
                        <ObjectListItem title="{data>name}" number="{data>scenario_id}" press="onSelectionChange"/>
                        
                    </items>
                </List>
            </Page>
        </pages>
    </App>
</mvc:View>